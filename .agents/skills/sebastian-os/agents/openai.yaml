interface:
  display_name: "Sebastian Os"
  short_description: "Repo-specific patterns and workflows for sebastian-os"
  default_prompt: "Use the sebastian-os repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true